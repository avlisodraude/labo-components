import CollectionConfig from './CollectionConfig';

export class ArtTubeConfig extends CollectionConfig {

	constructor(collectionId, stats, info) {
		super(collectionId, stats, info);
	}

}

export default ArtTubeConfig;